---
/**
 * Sobre Nosotros Section Component
 *
 * Displays information about Lola and Angela, the founders of Burjassot Llars.
 * Features:
 * - 2-column layout on desktop (image | content)
 * - Stacked layout on mobile (image on top)
 * - Three values with icons: Cercanía, Transparencia, Compromiso
 * - CTA button to full about page
 *
 * Following Design System: Terracota Mediterranea palette
 * Following HOME_DESIGN.md section "6. SOBRE NOSOTROS"
 *
 * SSG: This component is fully static with no client-side hydration needed.
 */

import { Heart, Shield, Users, ArrowRight } from '@lucide/astro';
import Button from '@/components/ui/Button.astro';

interface Value {
  icon: typeof Heart;
  iconBgClass: string;
  iconColorClass: string;
  title: string;
  description: string;
}

const values: Value[] = [
  {
    icon: Heart,
    iconBgClass: 'bg-primary/10',
    iconColorClass: 'text-primary',
    title: 'Cercanía',
    description: 'Escuchamos tus necesidades y adaptamos nuestro servicio a ti',
  },
  {
    icon: Shield,
    iconBgClass: 'bg-secondary/10',
    iconColorClass: 'text-secondary',
    title: 'Transparencia',
    description: 'Información clara y honesta en todo momento',
  },
  {
    icon: Users,
    iconBgClass: 'bg-accent/10',
    iconColorClass: 'text-accent',
    title: 'Compromiso',
    description: 'Te acompañamos desde la primera visita hasta después de la firma',
  },
];
---

<section
  id="sobre-nosotros"
  class="py-12 md:py-16 lg:py-20 bg-background"
  aria-labelledby="sobre-nosotros-heading"
>
  <div class="w-full max-w-7xl mx-auto px-4 sm:px-6 md:px-8 lg:px-12">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-center">
      <!-- Image Column -->
      <div class="order-2 lg:order-1">
        <div class="relative aspect-[4/3] lg:aspect-square overflow-hidden rounded-2xl">
          <!-- Placeholder for Lola and Angela photo -->
          <div
            class="w-full h-full bg-muted flex items-center justify-center"
            role="img"
            aria-label="Lola y Ángela, fundadoras de Burjassot Llars"
          >
            <div class="text-center p-8">
              <Users class="w-16 h-16 md:w-24 md:h-24 text-gray-400 mx-auto mb-4" aria-hidden="true" />
              <p class="text-gray-500 text-sm md:text-base">Foto de Lola y Ángela</p>
            </div>
          </div>

          <!-- Decorative Badge -->
          <div
            class="absolute bottom-4 right-4 md:bottom-6 md:right-6 bg-white/95 backdrop-blur-sm rounded-xl p-3 md:p-4 shadow-lg"
          >
            <p class="text-2xl md:text-3xl font-bold text-primary">10+</p>
            <p class="text-xs md:text-sm text-gray-600">Años de experiencia</p>
          </div>
        </div>
      </div>

      <!-- Content Column -->
      <div class="order-1 lg:order-2 space-y-6">
        <!-- Overline -->
        <p class="text-primary text-sm md:text-base font-medium uppercase tracking-wide">
          Sobre nosotras
        </p>

        <!-- Title -->
        <h2
          id="sobre-nosotros-heading"
          class="font-display text-3xl md:text-4xl font-bold text-gray-700 leading-tight"
        >
          Somos Lola y Ángela
        </h2>

        <!-- Description -->
        <div class="space-y-4 text-base md:text-lg text-gray-600 leading-relaxed">
          <p>
            Con más de 10 años de experiencia en el sector inmobiliario,
            nos especializamos en acompañar a familias en uno de los momentos más
            importantes de sus vidas: encontrar su hogar.
          </p>
          <p>
            Sabemos que comprar o vender una vivienda puede generar estrés e incertidumbre.
            Por eso, nuestro compromiso es estar a tu lado en cada paso, con transparencia
            total y el trato cercano que mereces.
          </p>
        </div>

        <!-- Values -->
        <div class="space-y-4 pt-4">
          {
            values.map((value) => {
              const IconComponent = value.icon;
              return (
                <div class="flex items-start gap-3">
                  <div
                    class={`flex items-center justify-center w-10 h-10 ${value.iconBgClass} rounded-lg flex-shrink-0`}
                  >
                    <IconComponent class={`w-5 h-5 ${value.iconColorClass}`} aria-hidden="true" />
                  </div>
                  <div>
                    <h3 class="font-semibold text-gray-700 mb-1">{value.title}</h3>
                    <p class="text-sm text-gray-600">{value.description}</p>
                  </div>
                </div>
              );
            })
          }
        </div>

        <!-- CTA -->
        <div class="pt-4">
          <Button variant="primary" size="md" as="a" href="/sobre-nosotros">
            Conocer más sobre nosotras
            <ArrowRight class="w-5 h-5" aria-hidden="true" />
          </Button>
        </div>
      </div>
    </div>
  </div>
</section>

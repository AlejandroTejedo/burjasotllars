---
/**
 * Hero Component
 *
 * Full-viewport hero section for the home page.
 * Features background with gradient overlay, main heading,
 * subtitle with value proposition, and CTAs.
 *
 * Following Design System: Terracota Mediterranea palette
 * Following HOME_DESIGN.md section "2. HERO SECTION"
 */

import { Search, ArrowRight, Star, CheckCircle, MapPin, ChevronDown } from '@lucide/astro';
import Button from '@/components/ui/Button.astro';
---

<section
  class="relative min-h-[calc(100vh-4rem)] md:min-h-[calc(100vh-5rem)] flex items-center overflow-hidden"
  aria-label="Sección principal"
>
  <!-- Background with gradient overlay -->
  <div class="absolute inset-0 z-0">
    <!-- Gradient background as placeholder (can be replaced with real image) -->
    <div
      class="w-full h-full bg-gradient-to-br from-gray-800 via-gray-700 to-gray-600"
      role="img"
      aria-label="Fondo decorativo de la sección hero"
    >
      <!-- Decorative pattern overlay -->
      <div class="absolute inset-0 opacity-10">
        <svg class="w-full h-full" xmlns="http://www.w3.org/2000/svg">
          <defs>
            <pattern id="hero-pattern" x="0" y="0" width="40" height="40" patternUnits="userSpaceOnUse">
              <circle cx="20" cy="20" r="1.5" fill="currentColor" class="text-white" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#hero-pattern)" />
        </svg>
      </div>
    </div>

    <!-- Gradient overlay for text legibility -->
    <div class="absolute inset-0 bg-gradient-to-r from-gray-900/80 via-gray-900/60 to-gray-900/40"></div>
  </div>

  <!-- Content -->
  <div class="relative z-10 w-full max-w-7xl mx-auto px-4 sm:px-6 md:px-8 lg:px-12 py-16 md:py-24">
    <div class="max-w-3xl animate-fade-in">
      <!-- Overline -->
      <p class="text-accent text-sm md:text-base font-medium uppercase tracking-wide mb-4">
        L&A Burjassot Llars
      </p>

      <!-- Main Title -->
      <h1 class="font-display text-4xl md:text-5xl lg:text-6xl font-bold text-white leading-tight mb-6">
        Encontramos el hogar que mereces
      </h1>

      <!-- Subtitle / Value Proposition -->
      <p class="text-lg md:text-xl text-white/90 leading-relaxed mb-8 md:mb-12 max-w-2xl">
        Un equipo familiar con más de 25 años de experiencia te acompaña en la búsqueda del hogar perfecto. Atención personalizada en cada paso, desde la primera visita hasta el momento de las llaves.
      </p>

      <!-- CTAs -->
      <div class="flex flex-col sm:flex-row items-stretch sm:items-center gap-4 mb-12">
        <Button
          as="a"
          href="/propiedades"
          variant="primary"
          size="lg"
          class="justify-center hover:shadow-lg"
        >
          <Search class="w-5 h-5" aria-hidden="true" />
          Buscar vivienda
        </Button>

        <a
          href="#servicios"
          class="inline-flex items-center justify-center gap-2 px-8 py-4 bg-transparent border-2 border-white text-white font-semibold text-lg rounded-lg transition-all hover:bg-white hover:text-gray-900 focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-900"
        >
          Nuestros servicios
          <ArrowRight class="w-5 h-5" aria-hidden="true" />
        </a>
      </div>

      <!-- Trust Indicators -->
      <div class="pt-8 border-t border-white/20">
        <div class="flex flex-wrap items-center gap-6 md:gap-8 text-white/80 text-sm">
          <div class="flex items-center gap-2">
            <Star class="w-5 h-5 text-accent" aria-hidden="true" />
            <span>25+ años de experiencia</span>
          </div>
          <div class="flex items-center gap-2">
            <CheckCircle class="w-5 h-5 text-accent" aria-hidden="true" />
            <span>200+ familias satisfechas</span>
          </div>
          <div class="flex items-center gap-2">
            <MapPin class="w-5 h-5 text-accent" aria-hidden="true" />
            <span>Profesionales de confianza</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2 hidden md:flex flex-col items-center gap-2 text-white/60 text-xs uppercase tracking-wide animate-bounce">
    <span class="sr-only">Desplázate para ver más contenido</span>
    <span aria-hidden="true">Descubre más</span>
    <ChevronDown class="w-6 h-6" aria-hidden="true" />
  </div>
</section>

<style>
  /* Animation for hero content entrance */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fadeInUp 0.6s ease-out forwards;
  }

  /* Respect user preference for reduced motion */
  @media (prefers-reduced-motion: reduce) {
    .animate-fade-in {
      animation: none;
      opacity: 1;
      transform: none;
    }

    .animate-bounce {
      animation: none;
    }
  }
</style>
